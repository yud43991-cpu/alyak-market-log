const users = [
 {username : buyer1,password :pill123}
 ];
 function login () {
  const inputUsername = document .getE1ementById (username) .value;
  constinputPassword= document.getE1ementById     (password).value;
  const messageDiv= document.getE1ementById(message);
);
if (user) {
messageDiv.style.color= green
messageDiv.textContent= 로그인 성공! 알약마켓으로 이동중...:
setTimeout (()=> {
  window.location.href= dashboard.html;//성공후 이동
  },1000);
  }else {
   messageDiv.style.color = red
   messageDiv.textContent= 로그인 실패, 아이디나 비밀번호가 틀렸어요. ;
   }
   }
